<div class="container">
    <div class="row">
        <div class="col-md-12">
            {{#each savedData}}
                <div class="panel panel-primary">
                    <div class="panel-heading row">
                        <div class="col-xs-6">
                            <h3 class="panel-title pull-left">{{this.title}}</h3>
                        </div>
                        <div class="col-xs-3">
                            <button data-articleid="{{this._id}}" id="deleteSaved" class="btn btn-default pull-right">DELETE FROM SAVED</button>
                            </form>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-xs-12>">
                            <div class="panel-body">
                                <p>{{this.link}}</p>
                            </div>
                        </div>
                    </div>
                </div>
            {{/each}}
        </div>
    </div>
</div>
